// @flow
/*
import React from 'react';
import Dropdown, { DropdownItem } from '../../../components/Dropdown';
import categories from '../../api/categories';

type Props = {};

type State = {
  isActive: boolean,
};

class CategoriesDropdown extends React.PureComponent<Props, State> {
  constructor(props: Props, context: null) {
    super(props, context);

    this.state = {
      isActive: false,
    };

    const self: any = this;
    self.onClickToItem = this.onClickToItem.bind(this);
    self.onClickToMe = this.onClickToMe.bind(this);
    self.onLeaveDropdown = this.onLeaveDropdown.bind(this);
  }

  onClickToItem(event: SyntheticEvent<HTMLElement>) {
    event.preventDefault();
    this.setActive(false);
  }

  onClickToMe(event: SyntheticEvent) {
    event.preventDefault();
    this.setActive(true);
  }

  onLeaveDropdown() {
    this.setActive(false);
  }

  setActive(isActive: boolean) {
    this.setState({
      isActive,
    });
  }
}
*/
