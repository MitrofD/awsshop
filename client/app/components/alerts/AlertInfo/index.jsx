// @flow
import React from 'react';
import Alert from '../Alert';
import './style.scss';

const AlertInfo = (props: Object) => (
  <Alert
    {...props}
    className="AlertInfo"
  />
);

export default AlertInfo;
