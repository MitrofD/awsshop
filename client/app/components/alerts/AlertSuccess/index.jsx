// @flow
import React from 'react';
import Alert from '../Alert';
import './style.scss';

const AlertSuccess = (props: Object) => (
  <Alert
    {...props}
    className="AlertSuccess"
  />
);

export default AlertSuccess;
