// @flow
import React from 'react';
import Alert from '../Alert';
import './style.scss';

const AlertWarning = (props: Object) => (
  <Alert
    {...props}
    className="AlertWarning"
  />
);

export default AlertWarning;
