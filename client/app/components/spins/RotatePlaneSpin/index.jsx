// @flow
import React from 'react';
import Spin from '../Spin';
import './style.scss';

const RotatePlaneSpin = (props: Object) => (
  <Spin
    {...props}
    className="RotatePlaneSpin"
  />
);

export default RotatePlaneSpin;
