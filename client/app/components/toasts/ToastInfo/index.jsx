// @flow
import React from 'react';
import Toast from '../Toast';
import './style.scss';

const ToastInfo = (props: Object) => (
  <Toast
    {...props}
    className="ToastInfo"
  />
);

export default ToastInfo;
